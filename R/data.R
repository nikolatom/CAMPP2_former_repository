#' dataset1 - example data
#'
#' A dataset1 containing 10000 gene counts for 25 samples (mix of tumor x normal samples). Data are derived from TCGA dataset.
#'
#' @format A data frame with 10000 rows (genes) and 25 columns (samples):
#' \describe{
#' }
#'
"dataset1"


#' dataset2 - example data
#'
#' A dataset2 containing 10000 gene counts for 25 samples (mix of tumor x normal samples). Data are derived from TCGA dataset.
#'
#' @format A data frame with 10000 rows (genes) and 25 columns (samples):
#' \describe{
#' }
#'
"dataset2"


#' metadata1 - example data
#'
#' A metadata for dataset1 containing 8 variables for 25 samples (mix of tumor x normal samples). Data are derived from TCGA dataset.
#'
#' @format A data frame with 25 rows (samples) and 9 columns.
#' \describe{
#' }
#'
"metadata1"


#' metadata2 - example data
#'
#' A metadata for dataset2 containing 8 variables for 25 samples (mix of tumor x normal samples). Data are derived from TCGA dataset.
#'
#' @format A data frame with 25 rows (samples) and 9 columns.
#' \describe{
#' }
#'
"metadata2"
